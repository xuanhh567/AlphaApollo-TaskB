# Phase 6 Plan: 回归、Trajectory 样例与 README

**Phase:** 6
**Status:** In Progress
**Language:** 中文
**Goal:** 保存结构化 tool call trajectory，整理回归实验记录，并更新 README，让 Task B/C 交付可检查、可解释、可复现。

## Requirements Covered

- COMPAT-05: 在 MATH-500 上完成 Task A baseline 与 Task B skill 版本回归对比，指标误差不超过 3%。
- COMPAT-06: 至少保存一个包含结构化 function-call 与 `<tool_response>` 的 trajectory 样例。
- DOC-01: `docs/task-b/learning-log.md` 记录阶段目标、改动和未理解问题。
- DOC-02: README 说明重构前后 tool call 流程对比和设计理由。
- DOC-03: README 或 `docs/task-b/experiments.md` 包含 Task A baseline 与 Task B 回归结果。
- DOC-04: Git 历史包含多次有意义 commit。

## Wave 1: 保存 Trajectory 样例

### Task 1.1: 生成 smoke trajectory

使用本地 `alphaapollo` 环境构造一个最小 informal math training env：

```text
question: What is 1 + 1?
action: <tool_call>{"name":"python_code","arguments":{"code":"print(1 + 1)"}}</tool_call>
```

保存：

```text
docs/task-b/trajectories/structured-python-code-smoke.md
```

### Task 1.2: 在 experiments 文档引用样例

新增：

```text
docs/task-b/experiments.md
```

记录 smoke test 命令、结果、限制。

## Wave 2: 回归实验记录

### Task 2.1: 写清楚 Task A / Task B 回归表

先创建表格：

```text
Task A baseline: pending
Task B skill version: pending
delta: pending
```

说明未运行原因：

```text
需要模型服务 / GPU / MATH-500 运行时间。
```

### Task 2.2: 写清楚建议运行命令

引用：

```text
examples/configs/rl_informal_math_tool.yaml
```

并记录后续需要补的日志路径。

## Wave 3: README Task B 专区

### Task 3.1: README 核心改动点

新增小节：

```text
Task B: Skill-based Tool Calling Refactor
```

内容：

- 重构前流程。
- 重构后流程。
- 主要文件。
- 如何运行测试。

### Task 3.2: README 复现和状态

说明：

- 单元测试命令。
- smoke trajectory 位置。
- MATH-500 回归状态。

## Wave 4: 状态收尾

### Task 4.1: 更新 requirements

可完成：

- COMPAT-06
- DOC-01
- DOC-02
- DOC-04

暂不完成：

- COMPAT-05
- DOC-03 如果没有真实回归结果，只能标 partial / pending。

### Task 4.2: 更新 learning log

记录 Phase 6 做了什么、哪些实验未跑。

## Phase 6 Completion Checklist

- [x] 保存 structured `<tool_call>` trajectory 样例。
- [x] 建立 `docs/task-b/experiments.md`。
- [x] README 新增 Task B 专区。
- [x] 记录当前测试命令和结果。
- [x] 明确 MATH-500 回归状态。
- [x] 更新 requirements / roadmap / learning log。
